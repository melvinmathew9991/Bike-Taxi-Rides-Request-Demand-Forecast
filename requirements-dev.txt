# Development dependencies. Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.4,<9.0
ruff>=0.4,<1.0

# Exploratory notebooks (not needed to run the pipeline or the tests).
jupyter>=1.0,<2.0
ipython>=8.0,<10.0
seaborn>=0.12,<1.0
